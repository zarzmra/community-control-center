# Shared UI Components

This directory contains reusable, domain-agnostic UI elements (often called design system primitives).

## Structure
- `ui/`: Core design primitives (Buttons, Inputs, Modals, Badges, Tables).
- `layout/`: Global layout components (Sidebar, Topbar, Footer, Container).
- `feedback/`: Loaders, alert banners, toast notifications.
